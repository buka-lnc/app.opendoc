{{#each
  (r__filter
    (r__complement (r__equals name))
    (r__uniq (r__map (ref-name) (pick-ref schema)))
  )
}}
import { {{this}} } from "./{{change-case @root.options.fileNamingStyle this}}"
{{#if @last}}


{{/if}}
{{/each}}


{{~> t_comments schema interface=name export=true}}
{{> t_interface this export=true}}
