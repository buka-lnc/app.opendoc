{{~#if items}}
{{~#if (r__isArray items)}}
{{!-- is tuple --}}
[
  {{~#each items~}}
  {{~> t_shape this}}
  {{~#unless @last}}, {{/unless~}}
  {{~/each~}}
]
{{~else~}}
({{> t_shape items}})[]
{{~/if~}}
{{~else~}}
({{> t_shape items}})[]
{{~/if~}}
