{{~#if (or schema.nullable (r__not (or (r__equals schema.type "object") (r__isObject schema.properties) schema.additionalProperties))) }}
{{#if export}}export {{/if}}type {{name}} = {{> t_shape schema}}
{{else}}
{{!-- only pure object --}}
{{#if export}}export {{/if}}interface {{name}} {{> t_shape schema}}
{{/if}}
