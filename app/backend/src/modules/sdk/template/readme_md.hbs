# {{fullName}}

## Install

```bash

npm install {{name}}@{{version}}
```

## Usage

```typescript
import { operationId } from '{{name}}'
import { operationId } from '{{name}}/hooks'
import { operationId } from '{{name}}/composables'

await operationId({
  // options
})
```
